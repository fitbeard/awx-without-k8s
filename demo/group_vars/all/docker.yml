docker_install: true

docker_daemon_options:
# Only "--network host" will work after that.
  bridge: none
  ip-forward: false
  iptables: false
  live-restore: true
  log-opts:
    max-file: '5'
    max-size: '50m'

# docker_daemon_options:
#  live-restore: true
#  log-opts:
#    max-file: '5'
#    max-size: '50m'
