{{- if .Values.kubeRbacProxy.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "eda-server-operator.fullname" . }}-metrics-reader
  labels:
    {{- include "eda-server-operator.labels" . | nindent 4 }}
rules:
  - nonResourceURLs:
      - /metrics
    verbs:
      - get
{{- end }}
