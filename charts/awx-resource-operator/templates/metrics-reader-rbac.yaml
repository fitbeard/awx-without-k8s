{{- if .Values.kubeRbacProxy.enabled -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "awx-resource-operator.fullname" . }}-metrics-reader
  labels:
    {{- include "awx-resource-operator.labels" . | nindent 4 }}
rules:
  - nonResourceURLs:
      - /metrics
    verbs:
      - get
{{- end }}
